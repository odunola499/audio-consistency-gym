[project]
name = "audio-consistency-models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets==2.21.0",
    "librosa",
    "lightning",
    "soundfile",
    "torch>=2.9.1",
    "torchaudio",
    "torchdiffeq",
    "transformers",
    "wandb",
]


[tool.black]
line-length = 88
target-version = ["py311"]

[tool.isort]
profile = "black"
line_length = 88
